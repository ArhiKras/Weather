## Weather CLI & Telegram Bot

Полнофункциональное погодное приложение с двумя интерфейсами:
- 💻 **CLI** - консольное приложение
- 🤖 **Telegram Bot** - полнофункциональный бот для Telegram

> 📖 Для документации по Telegram-боту смотрите [BOT_README.md](BOT_README.md)

### Структура проекта

Weather/

├── `CLI_app.py`          — точка входа в CLI-приложение  
├── `bot.py`              — Telegram-бот (🆕)  
├── `api_client.py`       — модуль для HTTP-запросов к OpenWeather (геокодинг и погода)  
├── `storage.py`          — модуль для управления кэшем и файловым I/O  
├── `CLI.py`              — модуль интерфейса командной строки (меню, ввод пользователя, вывод результата)  
├── `database/`           — папка с файлами базы данных (🆕)  
│   ├── `weather_cache.json`  — кэшированные данные о погоде  
│   └── `bot_users_data.json` — данные пользователей Telegram-бота  
├── `.cache/`             — папка с API кэшем (10 минут) (🆕)  
├── `requirements.txt`    — зависимости проекта  
├── `README.md`           — документация CLI  
├── `BOT_README.md`       — документация Telegram-бота (🆕)  
└── `.gitignore`          — игнорируемые файлы Git  

### Интерфейс (CLI)

После запуска `CLI_app.py` в терминале отображается главное меню:

**Главное меню:**
- **1 — Получить данные по городу**: переход в подменю для работы с городом
- **2 — Получить данные по координатам**: переход в подменю для работы с координатами
- **0 — Выход**: завершение работы программы

**Подменю (город или координаты):**
- **1 — Текущая погода**: запрос текущей погоды
  - Для города: если есть несколько одноимённых городов (до 3 вариантов), выводится погода для каждого варианта с указанием области/региона
  - Формат: `Погода в городе - <город> (<область>): <температура>°C, <описание>`
- **2 — Прогноз на 5 дней (каждые 3 часа)**: запрос почасового прогноза
  - Всего 40 записей (5 дней × 8 записей в день)
  - Отображается по 10 записей за раз с возможностью показать еще
  - Для каждой записи: дата/время, температура, описание, влажность, скорость ветра
- **3 — Качество воздуха**: запрос данных о загрязнении воздуха
  - Общий индекс качества воздуха (AQI)
  - Концентрация и индивидуальный индекс для каждого загрязняющего вещества
- **0 — Назад**: возврат в главное меню

При временных сетевых ошибках или коде ответа `429` запрос автоматически повторяется до 3 раз с паузами 1s, 2s, 4s.  
Если получить свежие данные не удалось, но есть актуальный кэш (младше 3 часов), приложение предложит использовать кэш:

- Вопрос: `Использовать данные из кэша (меньше 3 часов)? (y/n):`
- При ответе `y` выводятся данные из `weather_cache.json`.

### Инструкция по использованию

1. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```
2. **Настройка переменных окружения**
   - Создайте файл `.env` в корне проекта.
   - Добавьте строки:
     ```text
     API_KEY=ваш_ключ_openweather
     BOT_TOKEN=ваш_токен_telegram_бота
     ```
   - `API_KEY` - для работы с OpenWeather API
   - `BOT_TOKEN` - для работы Telegram-бота (получите у [@BotFather](https://t.me/botfather))
3. **Запуск приложения**
   
   **CLI версия:**
   ```bash
   python CLI_app.py
   ```
   
   **Telegram Bot:**
   ```bash
   python bot.py
   ```
   
   > 📖 Подробная документация по боту: [BOT_README.md](BOT_README.md)

### Примеры использования

4. **Работа с городом**
   - В главном меню выберите `1 — Получить данные по городу`
   - Введите название города, например: `Москва`
   - В подменю выберите нужное действие:
     - `1` — Текущая погода
       - При наличии нескольких совпадений вы увидите до 3 вариантов с разными регионами
       - Пример: `Погода в городе - Moscow (Moscow): 3°C, overcast clouds`
     - `2` — Прогноз на 5 дней
       - Отображается по 10 записей за раз
       - После каждой порции будет предложение "Показать еще 10 записей? (да/нет)"
     - `3` — Качество воздуха
       - Общий AQI и детальная информация по каждому веществу
       - Формат: `SO₂ (Диоксид серы): 0.73 мкг/м³ - Хорошо`
     - `0` — Назад в главное меню

5. **Работа с координатами**
   - В главном меню выберите `2 — Получить данные по координатам`
   - Введите широту и долготу, например:
     - Широта: `55.7558`
     - Долгота: `37.6173`
   - В подменю выберите нужное действие (аналогично работе с городом)

6. **Использование кэша**
   - Кэширование работает только для текущей погоды.
   - При сетевых проблемах (после нескольких попыток) программа спросит, использовать ли кэш.
   - При согласии (`y`) данные берутся из `weather_cache.json`, если они моложе 3 часов.
   - Прогноз на 5 дней и данные о качестве воздуха всегда запрашиваются заново.
